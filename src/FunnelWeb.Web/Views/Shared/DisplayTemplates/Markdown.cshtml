@model String

@if (ViewData.ContainsKey("Format") && (string)ViewData["Format"] == FunnelWeb.Model.Formats.Html)
{
  @MvcHtmlString.Create(Model)
}
else
{
  @Html.Markdown(Model, ViewData.ContainsKey("Sanitize") && (bool)ViewData["Sanitize"])
}
